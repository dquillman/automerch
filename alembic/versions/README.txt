Place autogenerated revision files here. Example usage:

  # create new migration
  alembic -c alembic.ini revision --autogenerate -m "add new columns"

  # apply to head
  alembic -c alembic.ini upgrade head

